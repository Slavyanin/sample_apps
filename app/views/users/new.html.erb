<h1>Присоединяйтесь!</h1>

<%= form_for(@user) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Присоединиться" %>
  </div>  
<% end %>
